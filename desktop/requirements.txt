# JARVIS Core Requirements
  fastapi==0.115.0
  uvicorn==0.30.6
  pydantic==2.9.2

  # System monitoring
  psutil==6.0.0

  # Voice — STT (Whisper, offline, Russian)
  openai-whisper==20231117

  # Voice — TTS (Microsoft Neural TTS — human-like Russian male voice, no API key needed)
  edge-tts==6.1.9

  # Voice — Wake word (Porcupine)
  pvporcupine==3.0.1

  # Audio
  pyaudio==0.2.14
  numpy==1.26.4

  # HTTP
  httpx==0.27.2
  aiofiles==23.2.1

  # Utilities
  python-dotenv==1.0.1
  cryptography==43.0.1

  # Icon generation (CI build step)
  pillow==10.4.0
  